#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include "monty.h"

/**
 * read_file - function to read a file
 * @filename: the file name
 * @limit: the size limit
 * Return: file content
 */
ops_t *read_file(const char *filename, size_t limit)
{
	if (filename == NULL || limit <= 0)
		return (NULL);

	return (NULL);
}
